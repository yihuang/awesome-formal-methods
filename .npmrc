# Pin the public npm registry so package-lock.json stays portable.
# Without this, a developer-level "registry" setting (mirrors, corporate proxies)
# gets baked into the lockfile as absolute "resolved" URLs, which then fail in
# CI and for anyone else. Regenerate with: rm -rf node_modules package-lock.json && npm install
registry=https://registry.npmjs.org/
